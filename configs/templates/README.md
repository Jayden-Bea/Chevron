# Template images

This folder is intentionally text-only to keep PR tooling compatible.

Add your own broadcast-specific template screenshots here, for example:
- `start.png`
- `stop.png`

Then point `templates.start` and `templates.stop` in `configs/example_config.yml` (or your own config) to those files.
